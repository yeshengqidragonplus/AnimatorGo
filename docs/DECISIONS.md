# 已定决策

记录已经做出的决策及其理由,**避免反复讨论**。

每条决策标注状态:
- **已定** —— 不要重新提议,除非有本文档未涵盖的新信息
- **推迟** —— 已评估,当前不做,列出了重新考虑的触发条件

日期为决策做出的日期。

---

## 架构

### 自建格式 + 每引擎一个运行时 · 已定 · 2026-08-06

编辑器输出自有格式,每个目标引擎有一个运行时负责解析和播放(Spine 的架构)。

**代价:永久维护 N 个运行时。已知晓并接受。**

**否决的替代方案:导出引擎原生动画格式。** 原因:原生格式只能承载「本工具特性 ∩ 目标格式特性」。网格形变在 Cocos Creator 上原生不可能,IK 在三个引擎上都得烘焙掉。走原生路线等于放弃这两个特性,或接受同一动画在不同引擎上表现不同。

不要因为「维护 N 个运行时成本高」重提原生导出 —— 那个成本是已经算过并接受的。

### 坐标系与色彩空间在导出时转换 · 已定 · 2026-08-06

编辑器内部统一约定(Y 向上、sRGB 非预乘),各引擎差异由**导出器**转换,运行时拿到什么用什么。

代价是资产要按引擎各导一份。取舍理由:运行时有 N 份,导出器只有一份逻辑,复杂度应该放在导出器。

### `core/` 不得依赖任何渲染 API · 已定 · 2026-08-06

硬约束。见 [ARCHITECTURE.md](ARCHITECTURE.md#core-与-render-分层)。

---

## 正确性标准

### 正确性的标准是肉眼 · 已定 · 2026-08-06

个人自用工具。**用户肉眼看不出问题 = 没问题。** 不追求跨引擎像素级一致。

### 黄金测试 · 推迟 · 2026-08-06

「dump 参考顶点输出、跨运行时逐值比对、容差断言」这类产品级基础设施**不前置搭建**。

**重新考虑的触发条件:** 真的遇到「某个引擎上动画不对,但看不出为什么」的情况。

---

## 格式与生态

### Spine 格式导出 · 已定不做 · 2026-08-06

用户已决定不做。

不要因为「技术上可行」「格式有公开文档」「已持有 Spine 授权」而重新提议。

### Live2D `.moc3` 导出 · 已定不做 · 2026-08-06

技术上不可行,不只是不想做:

1. `.moc3` 是闭源二进制,无公开规范
2. Cubism SDK 授权明文禁止逆向
3. **更根本的:模型不同构。** Live2D 是参数驱动的 deformer / morph 系统,不是骨骼蒙皮。即使能写出格式,本项目的数据模型也对不上

若将来想要类似的表现力,正确做法是在自有格式中增加 deformer 式形变 + 自有运行时,而不是导出 `.moc3`。

### DragonBones 格式 · 已定不用 · 2026-08-06

曾评估作为中间格式(理由:开源、有现成运行时)。**否决**:生态多年无人维护,Cocos Creator 3.x 支持不稳。用一个死掉的生态换一个活的没有意义。

若将来需要 DragonBones 兼容,作为众多导出器之一实现即可,不作为中间格式。

---

## 工具与依赖

### 不使用 PolyRig 等第三方付费插件 · 已定 · 2026-08-06

PolyRig($5.99,Godot 插件,Early Access)提供自动网格生成和智能权重,与本项目的网格模块功能重叠。

**不使用,自己实现。** 理由:

1. 所需算法全部是公开算法 —— CDT、Lloyd Relaxation、Geodesic IDW
2. 它只解决网格和权重,不含 IK、时间轴、切图导入
3. 闭源、Early Access、单人维护
4. **最关键的:它只服务 Godot。** 本项目要跨三个引擎,它帮不上忙

### 技术栈:TypeScript + PixiJS + React · 已定 · 2026-08-06

纯网页版,不打包。浏览器即跨平台,省掉打包与签名。需要本地文件系统读写时再用 Tauri 包一层,`core/` 不动。

**否决的替代方案:Rust + wgpu + egui。** 性能更好,但时间会消耗在渲染管线而非时间轴 UI 上 —— 而时间轴 UI 才是本项目的实际难点。

### License:MIT · 已定 · 2026-08-06

运行时库会被链进闭源商业游戏,需要宽松授权。GPL / AGPL 的传染性对运行时库是毒丸。
